# License notes

The workflow copies the upstream LiteRT-LM `LICENSE` file into the release package as:

```text
LICENSE-LiteRT-LM.txt
```

If upstream contains a `NOTICE` file, the workflow copies that too.

For a public project, review all generated artifacts and upstream dependency notices before publishing widely.
